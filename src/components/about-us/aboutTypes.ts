// components/about/types.ts

// Value item type
export type ValueItem = {
  icon: string;
  title: string;
  description: string;
};

// Team member type
export type TeamMember = {
  name: string;
  role: string;
  imagePath: string;
};

// Stat item type
export type StatItem = {
  count: string;
  label: string;
};
